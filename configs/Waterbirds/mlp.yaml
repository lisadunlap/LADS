EXP:
  ADVICE_METHOD: "ClipMLP"
  WANDB_SILENT: False
  PROJ: "Waterbirds"
  SEED: 0
  TEXT_PROMPTS: ['a photo of a {} on land.', 'a photo of a {} on water.']
  NEUTRAL_TEXT_PROMPTS: ['a photo of a {}.']
  EPOCHS: 200

DATA:
  DATASET: "Waterbirds"

METHOD:
  MODEL:
    NUM_LAYERS: 1
    DOM_WEIGHT: 1.0
    # LR: 0.005
    # WEIGHT_DECAY: 0.005
    CHECKPOINT: 'checkpoint/mlp.pth'
    CHECKPOINT_NAME: "mlp"
    RESUME: False
  USE_DOM_GT: True